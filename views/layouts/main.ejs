<!DOCTYPE html>
<html lang="en" class="html">

<head class="header">

  <title>Automotriz 911</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

  <!-- Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;700&display=swap" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/menu.css">

</head>

<body class="body">

  <nav class="menu">
    <div class="logo">
      <a class="logo_a"><img class="" src="/img/911.png"></a>
      <a class="logo_a">AUTOMOTRIZ 911</a>
    </div>
    <ul class="menu_items">
      <li class="menu_items-li"><a class="menu_items-a" href="/">INICIO</a></li>
      <li class="menu_items-li"><a class="menu_items-a" href="/servicios">SERVICIOS</a></li>
      <li class="menu_items-li"><a class="menu_items-a" href="/agencias">SUCURSALES</a></li>
      <li class="menu_items-li"><a class="menu_items-a" href="/contacto">CONTACTO</a></li>
      <li class="menu_items-li"><a class="menu_items-a" href="auth/signin" id="loginLink">INICIAR SESIÓN</a></li>
    </ul>

    <span class="boton_menu">
      <img class="span-img" src="/img/menu.png" alt="">
    </span>
  </nav>

  <%- body %>

    <script>
      // Obtén la URL de la página actual
      var currentPageUrl = window.location.href;

      // Obtén todos los enlaces dentro de la barra de navegación
      var navLinks = document.querySelectorAll('nav a');

      // Recorre los enlaces y compara sus URLs con la URL actual
      for (var i = 0; i < navLinks.length; i++) {
        var linkUrl = navLinks[i].href;

        // Si la URL actual coincide con la URL del enlace, agrega la clase "active"
        if (currentPageUrl === linkUrl) {
          navLinks[i].classList.add('active');
        }
      }
    </script>
    <script src="/scripts/menu.js"></script>


    <div>
      <% if (signupMessage.length> 0) { %>
        <div>
          <%= signupMessage %>
        </div>
        <% } %>

          <% if (signinMessage.length> 0) { %>
            <div>
              <%= signinMessage %>
            </div>
            <% } %>
    </div>

</body>

</html>